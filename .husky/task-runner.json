{
    "tasks": [
        {
            "name": "Run dotnet format on staged files",
            "group": "pre-commit",
            "pathMode": "relative",
            "command": "dotnet",
            "args": ["format", "YTSDownloader.sln", "--include", "${staged}"],
            "include": ["**/*.cs", "**/*.vb"]
        }
    ]
}
